"""You are given a list of binary values, where
each element is either 0 or 1. Your task is
to find all unique pairs of indices (i, j) such
that the values at those indices form the
specific pair [0, 1].
Input: A list of integers containing only 0s
and 1s.
Output: A list of tuples, each representing
a unique pair of indices that form the pair
[0, 1].
"""

